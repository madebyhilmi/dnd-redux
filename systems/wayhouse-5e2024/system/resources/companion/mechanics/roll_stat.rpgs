mechanic roll_stat(event_names = "companion_list_roll_stat") =
  roll(
    rolls = [
      rollable(
        formulas = [concat(["1d20 + ", metaStat(meta_stat = $event.stat)])],
        text = $event.statName,
      ),
    ],
  );
